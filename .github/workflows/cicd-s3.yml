name: cicd-s3

on:
    push:
        branches: [develop]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3
            
            - name: Build React App
              run: npm install && npm run build

